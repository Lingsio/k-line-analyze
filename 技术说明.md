# 项目技术说明：大规模金融图像数据的渲染与 CNN 训练

> **论文来源**: JOFI — *"Re-imagining Price Trends"*
> **核心任务**: 将股票 OHLC 行情数据转换为灰度 K 线图像 → 使用 CNN 预测未来收益方向

---

## 目录

- [1. 项目概览](#1-项目概览)
- [2. 数据规模](#2-数据规模)
- [3. 图像渲染管线](#3-图像渲染管线)
- [4. CNN 训练管线](#4-cnn-训练管线)
- [5. 整体流程图](#5-整体流程图)
- [6. 复现建议](#6-复现建议)

---

## 1. 项目概览

### 1.1 代码结构

```
trend_code_submit/
├── Data/                      # 数据处理与图像生成
│   ├── dgp_config.py          # 全局配置（图像尺寸、颜色、路径）
│   ├── equity_data.py         # CRSP 原始数据预处理
│   ├── chart_library.py       # PIL 图像绘制核心类
│   ├── chart_dataset.py       # PyTorch Dataset（CNN2D + CNN1D）
│   └── generate_chart.py      # 按年批量生成图像的主控类
├── Model/
│   └── cnn_model.py           # CNN2D 和 CNN1D 模型定义
├── Experiments/
│   └── cnn_experiment.py      # 训练/评估/集成预测主流程
├── Portfolio/
│   └── portfolio.py           # 多空组合构建
├── Analysis/                  # 论文表格/图表复现
├── Misc/
│   ├── config.py              # 模型超参数与路径配置
│   ├── cache_manager.py       # 缓存管理
│   └── utilities.py           # 工具函数
├── sample_codes.py            # 示例入口脚本
├── cnn_env.yml                # Conda 环境配置
└── README.txt                 # 项目说明
```

### 1.2 环境依赖

```yaml
# cnn_env.yml 关键依赖
python: 3.8.13
pytorch: 1.7.1
torchvision: 0.8.2
pillow: 8.2.0
pandas: 1.5.0
numpy: 1.23.3
matplotlib: 3.3.4
scikit-learn: 1.1.1
```

安装命令：

```bash
conda env create -f cnn_env.yml -p ../cnn_env
conda activate ../cnn_env
```

---

## 2. 数据规模

| 维度 | 数值 |
|------|------|
| 时间跨度 | 1993–2019（27 年） |
| 美股数量/年 | ~3,000–8,000 只 |
| 采样频率 | 每月末（`freq="month"`）≈ 12 次/年/股 |
| 单年样本量 | ~50,000–90,000 张图像 |
| **总样本量** | **~150 万–250 万张图像** |
| 国际市场 | 额外 26 个国家 |

### 样本期与预测期划分

| 类型 | 年份 | 用途 |
|------|------|------|
| 样本内（IS） | 1993–2000（8 年） | 训练 + 验证 |
| 样本外（OOS） | 2001–2019（19 年） | 独立测试 |

> 来源: `config.py` — `IS_YEARS = list(range(1993, 2001))`, `OOS_YEARS = list(range(2001, 2020))`

---

## 3. 图像渲染管线

### 3.1 极小分辨率 + 纯灰度

**这是效率的核心**。图像分辨率极小，远低于常规 CNN 输入：

| 窗口期 (ws) | 宽度 (px) | 高度 (px) | 总像素 | 对比 ImageNet 224×224 |
|---|---|---|---|---|
| 5 天 | 15 | 32 | 480 | 小 **312 倍** |
| 20 天 | 60 | 64 | 3,840 | 小 **39 倍** |
| 60 天 | 180 | 96 | 17,280 | 小 **8.7 倍** |

- **颜色模式**: 灰度（`"L"`），单通道
- **像素值**: 背景 = 0（黑），前景 = 255（白）
- **Bar 宽度**: 固定 3 像素（`BAR_WIDTH = 3`）

> 来源: `dgp_config.py`
>
> ```python
> IMAGE_WIDTH = {5: 15, 20: 60, 60: 180}   # BAR_WIDTH × 天数
> IMAGE_HEIGHT = {5: 32, 20: 64, 60: 96}
> ```

### 3.2 渲染方式：PIL 像素级绘制

项目使用 `PIL.Image` 直接操作像素，**不使用 matplotlib**：

```python
# chart_library.py — DrawOHLC.__draw_ohlc()

# 1. 创建空白灰度画布
ohlc = Image.new("L", (width, height), BACKGROUND_COLOR)  # 全黑
pixels = ohlc.load()  # 获取像素访问对象

# 2. 绘制 OHLC bar — 逐像素设置
for day in range(ohlc_len):
    # 高低线：竖线
    for j in range(line_bottom, line_up + 1):
        pixels[center_x, j] = 255
    # 开盘线：左半横线
    for i in range(left, center_x + 1):
        pixels[i, open_y] = 255
    # 收盘线：右半横线
    for i in range(center_x + 1, right + 1):
        pixels[i, close_y] = 255

# 3. 绘制移动平均线
draw = ImageDraw.Draw(ohlc)
draw.line((x1, y1, x2, y2), width=1, fill=255)

# 4. 绘制成交量柱
volume_bar = Image.new("L", (width, volume_height), 0)
# ...同样逐像素设置

# 5. 拼接 OHLC + Volume
image = Image.new("L", (width, total_height))
image.paste(ohlc, (0, volume_height + gap))
image.paste(volume_bar, (0, 0))
image = image.transpose(Image.FLIP_TOP_BOTTOM)  # 翻转 Y 轴
```

**为什么不用 matplotlib？**

| 方面 | matplotlib `savefig()` | PIL 像素操作 |
|------|---|---|
| 创建开销 | 创建 Figure/Axes/Renderer | 仅创建 Image 对象 |
| 渲染 | DPI 光栅化管线 | 直接写像素坐标 |
| I/O | 保存为 PNG 文件 | 留在内存中 |
| 速度 | 每张 ~50–200ms | 每张 ~0.1–1ms |
| **百万张总耗时** | ~14–55 小时 | **~数分钟–数十分钟** |

### 3.3 价格归一化

在绘图前，对 OHLC 价格进行归一化处理（`adjust_price` + 窗口内归一化）：

```python
# generate_chart.py — adjust_price()
# 步骤1：用收益率重建调整后的价格序列
res_df["Close"] = (1 + today_ret) * pre_close

# 步骤2：窗口起始日 Close 标准化为 1.0
data[["Open", "High", "Low", "Close"]] *= (1.0 / data["Close"].loc[start])

# 结果：所有价格围绕 1.0 浮动，消除价位差异
```

### 3.4 存储方式：`numpy.memmap` 二进制文件

所有图像**不存为单独 PNG 文件**，而是展平后写入一个连续二进制文件：

```python
# generate_chart.py — save_annual_data()

# 1. PIL Image → numpy 一维数组
im_arr = np.frombuffer(image.tobytes(), dtype=np.uint8)  # 例如 shape=(3840,)

# 2. 存入预分配数组
data_dict["image"][sample_num, :] = im_arr[:]

# 3. 写入 memmap 文件
fp_x = np.memmap("xxx_images.dat", dtype=np.uint8, mode="w+", shape=(N, W*H))
fp_x[:] = data_dict["image"][:]
del fp_x

# 4. 标签数据 → feather 文件（高效列式存储）
pd.DataFrame(label_data).to_feather("xxx_labels.feather")
```

**文件组织（按年分片）**：

```
stocks_USA/dataset_all/
├── 20d_month_has_vb_[20]_ma_1993_images.dat      # ~50K × 3840 bytes ≈ 183MB
├── 20d_month_has_vb_[20]_ma_1993_labels.feather   # 标签 + 元数据
├── 20d_month_has_vb_[20]_ma_1993.txt              # 生成日志
├── 20d_month_has_vb_[20]_ma_1994_images.dat
├── ...
└── 20d_month_has_vb_[20]_ma_2019_images.dat
```

**按年分片的优势**：

- ✅ 可并行生成不同年份
- ✅ 单次内存峰值可控
- ✅ 某年出错/更新不影响其他年
- ✅ 支持中断后恢复（自动跳过已生成的年份）

### 3.5 支持的图表类型

| 类型 | 说明 | 绘制方式 |
|------|------|---------|
| `"bar"` | 标准 OHLC 棒线图 | 实心竖线 + 左开/右收横线 |
| `"pixel"` | 像素化图 | 仅绘制关键像素点 |
| `"centered_pixel"` | 中心化像素图 | 每日价格以自身 Close 为中心 |

---

## 4. CNN 训练管线

### 4.1 数据加载：内存映射 Dataset

```python
# chart_dataset.py — EquityDataset

class EquityDataset(Dataset):
    def load_image_np_data(img_path, ohlc_len):
        # 只读 memmap，OS 按需换页加载
        images = np.memmap(img_path, dtype=np.uint8, mode="r")
        images = images.reshape((-1, 1, HEIGHT, WIDTH))  # (N, 1, H, W)
        return images

    def __getitem__(self, idx):
        # 逐样本读取（仅触发对应页的磁盘 I/O）
        image = (self.images[idx] / 255.0 - mean) / std
        return {"image": image, "label": label, ...}
```

**关键点**：

- `mode="r"` 只读映射 → 不复制到 RAM
- 访问 `images[idx]` 时 OS 才将对应磁盘页换入内存
- 250 万张图（~580MB，20 天窗口）完全可以被 memmap 处理

### 4.2 数据标准化

```python
# 使用前 50,000 个样本估计 mean 和 std
mean, std = images[:50000].mean() / 255.0, images[:50000].std() / 255.0
# 缓存到 .npz 文件避免重复计算

# getitem 中 z-score 标准化
image = (self.images[idx] / 255.0 - mean) / std
```

### 4.3 DataLoader 配置

```python
# 训练集：70/30 随机划分
train_size = int(len(tv_dataset) * 0.7)
train_dataset, val_dataset = random_split(tv_dataset, [train_size, val_size])

# DataLoader
DataLoader(train_dataset, batch_size=128, shuffle=True, num_workers=1)
```

| 参数 | 值 | 说明 |
|------|---|------|
| `batch_size` | 128 | 每轮迭代仅读取 128 样本 |
| `shuffle` | True | 训练集打乱顺序 |
| `num_workers` | 1 | memmap 本身足够快，单进程足矣 |

### 4.4 CNN 模型架构

#### CNN2D（主模型）

| 窗口期 | 层数 | 输入尺寸 | 结构 |
|---|---|---|---|
| 5 天 | 2 | 1×32×15 | `[Conv2d(64) → BN → LReLU → MaxPool]×2 → Dropout → FC(2)` |
| 20 天 | 3 | 1×64×60 | `[Conv2d(64) → BN → LReLU → MaxPool]×3 → Dropout → FC(2)` |
| 60 天 | 4 | 1×96×180 | `[Conv2d(64) → BN → LReLU → MaxPool]×4 → Dropout → FC(2)` |

```python
# cnn_model.py — CNNModel
Conv2d(1, 64, kernel=(5,3), stride=(1,1), padding=(2,1))  # 第一层 stride 可能为(3,1)
→ BatchNorm2d(64)
→ LeakyReLU
→ MaxPool2d((2,1), ceil_mode=True)
→ ... (重复)
→ Flatten
→ Dropout(p=0.5)
→ Linear(flatten_size, 2)   # 二分类: 涨/跌
```

#### CNN1D（对比模型）

```python
# 输入: (batch, 6, window_size)
# 6 通道 = Open, High, Low, Close, MA, Volume
Conv1d(6, 64, kernel=3) → BN1d → LReLU → MaxPool1d(2)
→ ... (1-3层)
→ Flatten → Dropout → FC(2)
```

### 4.5 训练策略

```python
# cnn_experiment.py — Experiment 类

# 优化器
optimizer = optim.Adam(model.parameters(), lr=1e-4)

# 损失函数
loss = CrossEntropyLoss()(outputs, labels)  # 分类任务
# 或 MSELoss()（回归任务）

# Early Stopping
if (epoch - best_epoch) >= 2:
    break  # 验证 loss 连续 2 epoch 无改善

# Ensemble: 训练 5 个独立模型
for model_num in range(5):
    train_single_model(...)
    torch.save(best_model, f"checkpoint{model_num}.pth.tar")

# 预测时取 5 个模型 softmax 概率的平均
total_prob = sum(softmax(model_i(x)) for i in range(5)) / 5
```

### 4.6 训练性能指标

```python
# 每个 epoch 记录
- loss:     交叉熵损失
- accy:     准确率
- MCC:      Matthews 相关系数
- diff:     预测偏差（预测涨-预测跌占比）
```

### 4.7 GPU / CPU 兼容

```python
device = torch.device("cuda" if torch.cuda.is_available() else "cpu")
model.to(device)
cudnn.benchmark = True  # cuDNN 自动调优

# 数据也发送到同一设备
inputs = batch["image"].to(device, dtype=torch.float)
labels = batch["label"].to(device, dtype=torch.long)
```

---

## 5. 整体流程图

```
┌─────────────────────────────────────────────────────────────────┐
│                        Phase 1: 图像生成                         │
│                                                                   │
│  CRSP 原始数据                                                    │
│       │                                                           │
│       ▼                                                           │
│  equity_data.py ── 按年加载 + 计算收益/波动率/市值                 │
│       │                                                           │
│       ▼                                                           │
│  generate_chart.py ── 遍历每只股票 × 每个月末日期                  │
│       │                                                           │
│       ├── adjust_price() ── 价格归一化                            │
│       │                                                           │
│       ├── chart_library.py ── DrawOHLC()                          │
│       │       │                                                   │
│       │       ├── PIL Image.new("L") ── 创建灰度画布              │
│       │       ├── pixels[x,y] = 255 ── 逐像素绘制 OHLC           │
│       │       ├── draw.line() ── 绘制移动平均线                   │
│       │       └── 拼接成交量柱 ── paste()                         │
│       │                                                           │
│       ├── image.tobytes() → np.frombuffer() ── 转 numpy 数组     │
│       │                                                           │
│       └── np.memmap() 写入 .dat 文件                              │
│           pd.to_feather() 写入标签                                │
│                                                                   │
│  输出: xxx_images.dat + xxx_labels.feather （每年一对文件）        │
└─────────────────────────────────────────────────────────────────┘
                              │
                              ▼
┌─────────────────────────────────────────────────────────────────┐
│                        Phase 2: CNN 训练                         │
│                                                                   │
│  chart_dataset.py ── EquityDataset                                │
│       │                                                           │
│       ├── np.memmap(mode="r") ── 只读内存映射加载                 │
│       ├── reshape(-1, 1, H, W) ── 重组为图像 tensor              │
│       └── z-score 标准化 ── (pixel/255 - mean) / std             │
│                                                                   │
│  cnn_experiment.py ── Experiment                                  │
│       │                                                           │
│       ├── ConcatDataset(IS 年份) ── 合并 1993-2000 数据           │
│       ├── random_split(0.7/0.3) ── 训练/验证集划分                │
│       ├── DataLoader(batch=128) ── mini-batch 迭代器              │
│       │                                                           │
│       ├── × 5 Ensemble 循环:                                     │
│       │   ├── CNNModel ── 2-4 层 Conv2d + BN + LReLU + MaxPool   │
│       │   ├── Adam(lr=1e-4) ── 优化器                            │
│       │   ├── CrossEntropyLoss ── 损失函数                        │
│       │   ├── Early Stopping (patience=2)                         │
│       │   └── 保存 checkpoint{i}.pth.tar                          │
│       │                                                           │
│       └── OOS 预测:                                               │
│           ├── 加载 5 个模型                                       │
│           ├── softmax 概率平均                                    │
│           └── 生成 ensem_res.csv                                  │
│                                                                   │
│  输出: 逐股票逐日期的上涨概率预测 → 构建多空组合                   │
└─────────────────────────────────────────────────────────────────┘
```

---

## 6. 复现建议

### 6.1 快速测试

```python
# 只生成一年数据测试管线
year_list = [2019]
ws = 20
freq = "month"
chart_type = "bar"

for year in year_list:
    dgp_obj = GenerateStockData("USA", year, ws, freq,
                                 ma_lags=[ws], volume_bar=True,
                                 chart_type=chart_type)
    dgp_obj.save_annual_data()
```

### 6.2 硬件需求

| 配置 | 图像生成 | CNN 训练（单次 5 ensemble） |
|------|---------|--------------------------|
| CPU only | 可行，~1-2 小时/年 | 可行但慢，~数小时 |
| GPU (RTX 3060+) | N/A（CPU 任务） | ~30 分钟–1 小时 |
| RAM | ≥ 8GB 即可 | ≥ 8GB 即可（memmap） |
| 磁盘 | ~5–20 GB（全部年份） | checkpoint ~几百 MB |

### 6.3 注意事项

1. **原始数据**：需要 CRSP 数据库的美股日频数据（学术授权）
2. **CACHE_DIR.zip**：包含预计算的中间结果，解压后放在 `trend_submit/` 下
3. **GPU 选择**：`os.environ["CUDA_VISIBLE_DEVICES"] = "0"` 指定 GPU 编号
4. **中断恢复**：代码自动跳过已生成的图像文件和已训练的模型

---

## 附录：关键参数速查

| 参数 | 默认值 | 位置 | 说明 |
|------|------|------|------|
| `BATCH_SIZE` | 128 | `config.py` | 训练 batch 大小 |
| `TRUE_DATA_CNN_INPLANES` | 64 | `config.py` | CNN 初始通道数 |
| `NUM_WORKERS` | 1 | `config.py` | DataLoader 进程数 |
| `BAR_WIDTH` | 3 | `dgp_config.py` | 每根 K 线像素宽度 |
| `VOLUME_CHART_GAP` | 1 | `dgp_config.py` | OHLC 与成交量区间距 |
| `ensem` | 5 | `cnn_experiment.py` | Ensemble 模型数量 |
| `max_epoch` | 50 | `cnn_experiment.py` | 最大训练轮数 |
| `train_size_ratio` | 0.7 | `cnn_experiment.py` | 训练/验证比例 |
| `drop_prob` | 0.50 | `cnn_model.py` | Dropout 概率 |
| `early_stop` 容忍度 | 2 epochs | `cnn_experiment.py` | 验证无改善即停止 |
